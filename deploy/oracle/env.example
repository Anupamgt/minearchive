# Copy to .env on the VM (never commit the real file).
# setup-vm.sh generates a password if you leave this placeholder.

POSTGRES_USER=minearchive
POSTGRES_PASSWORD=change-me-to-a-long-random-string
POSTGRES_DB=minearchive_db
POSTGRES_PORT=5432
PGBOUNCER_PORT=6543
